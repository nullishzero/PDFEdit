<sect1 id="gui_settings_commandline" xreflabel="settings of command console">
	<title>Commandline tab</title>

	<para>
			Looks command editor is settings in options dialog on tab "commandline".
			To execute options dialog click to menu on Tools/Options. Or type to
			command editor <emphasis>options()</emphasis>. Or click to options icon in toolbars.
			<mediaobject>
				<imageobject>
					<imagedata fileref="gui/images/options_commandline.png" format="PNG"/>
				</imageobject>
				<textobject>
					<phrase>Commandline tab</phrase>
				</textobject>
			</mediaobject>
	</para>

	<sect2>
		<title>Show return value of executed script in console</title>
		<para>
				If this setting is checked, command console will print return value of last command
				after executing it. It has basically the same effect as if last command was a
				parameter of function <function>print()</function>.
		</para>
	</sect2>
	<sect2>
		<title>Show return value also for complex type</title>
		<para>
				This will toggle showin the return value for complex types, such as arrays,
				which are likely to produce lot of output, as arrays tend to be large.
		</para>
	</sect2>
	<sect2>
		<title>Show errors from event handlers</title>
		<para>
				This specify if you want to see error from callback handlers.
				If there is error in callback handler that is called very often,
				like handler for current item in tree being changes, it can produce
				lot out output.
		</para>
	</sect2>
	<sect2>
		<title>Show command editor</title>
		<para>
				This option set command editor shown, if is check.
				<mediaobject>
					<imageobject>
						<imagedata fileref="gui/images/command_options_cmdedit.png" format="PNG"/>
					</imageobject>
					<textobject>
						<phrase>Command console without / with command editor</phrase>
					</textobject>
					<caption><para>Command console without / with command editor</para></caption>
				</mediaobject>
		</para>
	</sect2>
	<sect2>
		<title>Show command line</title>
		<para>
				This option set shown history combobox, if is check. This is usefull,
				if user want very old histry command.
		</para>
	</sect2>
	<sect2>
		<title>Editable command line</title>
		<para>
				This option set shown editable command line in combobox of history, if is check.
				This is usefull, if user want small space for typing command (set command editor
				hiden).
		</para>
	</sect2>
	<sect2>
		<title>History options</title>
		<para>
				This options set where will be saved and loaded command history and how much command in the
				past are saving.
		</para>
		<para>
				Note: History is saving after close PDFedit's window.
		</para>
	</sect2>
</sect1>
